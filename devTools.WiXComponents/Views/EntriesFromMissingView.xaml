<UserControl
	x:Class="devTools.WiXComponents.Views.EntriesFromMissingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="clr-namespace:devTools.WiXComponents.Controls;assembly=WiXComp.Controls" 
	mc:Ignorable="d" 
    d:DesignWidth="800" d:DesignHeight="500"
	d:DataContext="{d:DesignData EntriesFromMissingViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<ItemsControl Grid.IsSharedSizeScope="True">
			<controls:FormControlItem Label="Root Path:">
				<controls:SelectFolder SelectedPath="{Binding RootPath}" />
			</controls:FormControlItem>
			<controls:FormControlItem Label="Path:">
				<controls:SelectFolder SelectedPath="{Binding Path}" />
			</controls:FormControlItem>
			<controls:FormControlItem Label="File Name:">
				<controls:OpenFile FileName="{Binding FileName}" 
									Filter="Supported Files|*.wsx;*.wsi;*.xml;*.csproj;*.vbproj|WiX Toolset files|*.wsx;*.wsi|XML Files|*.xml|Project Files|*.csproj;*.vbproj|All Files|*.*" />
			</controls:FormControlItem>
		</ItemsControl>
	</Grid>
</UserControl>
